1. 重新检查 `web/public/` 中目标 PNG/ICO 的当前工作树状态、尺寸与透明通道，确认不覆盖用户已有的图标内容；始终排除 `screenshot-desktop.png`、`screenshot-mobile.png`、`default-avatar.png`。

2. 对普通品牌与浏览器资源制作透明的柔和圆角位图（圆角半径约为边长的 12%）：`Ech0.png`、`rEch0.png`、Android Chrome 图标、`web-app-manifest-*` 图标、`favicon-*.png`，并重新生成含圆角透明角的 `favicon.ico`。`favicon-96x96.png` 会从 RGB 转为支持透明通道的 PNG。

3. 按你的选择保留 `maskable-icon.png` 与 `apple-touch-icon.png` 的方形原图：前者需要完整画布供 Android 启动器遮罩，后者由 iOS 自行应用圆角。三张指定排除图不作改动。

4. 用图像元数据和像素检查确认每个处理资源保持原尺寸、PNG/ICO 可读、圆角透明且中心像素内容未被裁切；同时核对 `web/public` 引用和工作树差异只包含预期的二进制资产。

5. 运行与静态资源相关的 Go 测试及 `pnpm -C web build`，然后提交并推送该品牌图标圆角改动，不包含 `.zcode/` 或任何用户未要求的文件。